apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: download-trigger
  namespace: image-processing
spec:
  broker: image-processing-broker
  filter:
    attributes:
      type: dev.knative.samples.image.download.requested
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: downloader
---
apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: process-trigger
  namespace: image-processing
spec:
  broker: image-processing-broker
  filter:
    attributes:
      type: dev.knative.samples.image.download.completed
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: processor
---
apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: error-trigger
  namespace: image-processing
spec:
  broker: image-processing-broker
  filter:
    attributes:
      type: dev.knative.samples.image.error
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: error-handler
